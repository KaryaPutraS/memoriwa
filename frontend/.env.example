# Backend API base URL (defaults to same-origin if unset)
VITE_API_URL=http://localhost:8000

# WebSocket URL (defaults to VITE_API_URL with ws:// if unset)
VITE_WS_URL=ws://localhost:8000

# Set to true to use built-in demo data without a backend
VITE_DEMO_MODE=false
